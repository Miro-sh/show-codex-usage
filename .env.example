# URL of the service that returns your quota data.
USAGE_API_URL=https://example.com/api/usage

# Keep the real key in .env. It is sent by the server as X-API-KEY.
USAGE_API_KEY=replace-with-your-key

# Optional. Defaults to 3000.
PORT=3000
